<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="com.gallery.mail">
    <insert id="addKey" parameterType="mailVo">
        INSERT INTO pw_key
        	(
        	pwkey
        	,cstmr_id
        	,regtime
        	) 
        VALUES (
        	#{pwkey}
        	, #{cstmrId}
        	, now()
        	)    
    </insert>
    
    <delete id="destroyKey" parameterType="mailVo">
    	DELETE from pw_key
    	WHERE
    		cstmr_id=#{cstmrId}
    </delete>
    <select id="getPwKeyCnt" parameterType="mailVo" resultType="Integer">
    	SELECT count(*) from pw_key
    	where pwkey=#{pwkey}
    </select>
 	<select id="getCstmrForKey" parameterType="mailVo" resultType="cstmrVo">
    	SELECT cstmr_id from pw_key
    	where pwkey=#{pwkey}
    </select>
    
    
</mapper>