<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="com.gallerytalk.salePrdct">
	<select id="getDlvryCheck" parameterType="SalePrdctVo" resultType="Integer">
	    SELECT
			dlvry
		FROM
			salePrdct_off
		WHERE
			sale_id=#{saleId}
		AND
			prdct_id=#{prdctId}
		AND
			itemTy = #{itemTy}
	</select>
	
</mapper>